<h1>My Tasks</h1>
<ul style = "list-style: none;">
  <% @tasks.each do |task| %>
  <li>
     <%= task.completed %>
     <%= link_to task.title, task_path(task.id) %>
  </li>
  <% end %>
</ul>

<%= link_to "Create a new task", new_task_path %>

